{
  "description": "Check for BSL Language Server installation on session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/check-bsl-ls.sh",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
